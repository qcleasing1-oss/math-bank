#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""
ด่าน 15 — ตัวเลือกในข้อเดียวกันต้องไม่ซ้ำกัน
============================================

**ปัญหาที่ด่านนี้เกิดมาแก้ (9 ส.ค. 69)**

`chap-13-calculus-q135` มี `notes` เตือนไว้ว่า *"ตัวเลือก 2 กับ 4 ในสแกนอ่านเหมือนกัน"*
MB ไล่ตามบรรทัดนั้นแล้วกวาดทั้งคลัง — เจอข้อที่สอง **`chap-13-calculus-q194`** ซึ่ง
⛔ ไม่มีธง `flawedSource` · ⛔ ไม่มี `notes` ที่พูดถึงการซ้ำ · ⛔ ไม่อยู่ในตระกูลป้ายใด

🔴 **จุดที่น่ากลัวคือ ⛔ ไม่มีด่านไหนใน 20 ด่านที่ถามคำถามนี้**
   `q194` ถูกเจอเพราะ *มีคนไล่ตามข้ออื่นบังเอิญผ่านมา* ⛔ ไม่ใช่เพราะระบบเฝ้าเห็น
   ⇒ ⇒ ด่านนี้เปลี่ยน **"เจอเพราะบังเอิญ"** ให้เป็น **"เจอโดยโครงสร้าง"** (E→MB #59 ③)

**กฎที่บังคับ**

  ①  ข้อใดมีค่าใน `choices` ซ้ำกัน **ทุกไบต์** ⇒ 🔴 แดง
      เว้นแต่ `id` นั้นถูกปักหมุดไว้ใน `KNOWN_DUPES` พร้อมคู่ดัชนีและข้อความที่ตรงกันเป๊ะ
  ②  🔑 **หมุดที่ไม่มีของรองรับแล้ว ⇒ แดง** — ข้อหาย · ตัวเลือกไม่ซ้ำแล้ว · คู่ดัชนีเปลี่ยน
      ⇒ รายการนี้ **ย่อได้อย่างเดียว** (ทรงเดียวกับ `ALLOW_BARE` ของด่าน 14)
  ③  เหตุผลของหมุดต้องยาว ≥ MIN_REASON ตัวอักษร (เหตุผลเปล่า = การเลี่ยงกฎ)
  ④  ตัวหารต้องไม่เป็นศูนย์ — สแกนแล้วไม่เจอข้อชนิดมีตัวเลือกเลย ⇒ แดง
      (⛔ "เขียวเพราะไม่ได้ตรวจอะไร" ต้องไม่มีหน้าตาเหมือน "เขียวเพราะสะอาด")

**⛔ ทำไมเทียบ "ทุกไบต์" ⛔ ไม่ใช่เทียบแบบผ่อนปรน**

  วัดแล้ว 9 ส.ค. 69 บน `data/sets` (4,370 ข้อชนิดมีตัวเลือก) — สามกฎให้ผลเท่ากันหมด:
      เป๊ะทุกไบต์ ........................................ 2 ข้อ
      ตัดช่องว่างทั้งหมด .................................. 2 ข้อ
      ตัดช่องว่าง + dfrac→frac + left/right + $ ........... 2 ข้อ
  ⇒ กฎกว้างวันนี้ **ไม่ได้จับเพิ่มเลย** แต่แลกมาด้วยความเสี่ยงเท็จบวกในอนาคต
  ⇒ ⇒ เลือกกฎแคบเป็น **คำตัดสิน** · กฎกว้างเป็น **ตัวเฝ้าความสอดคล้อง** (พิมพ์ ⚠️ ⛔ ไม่แดง)
     ถ้าวันหนึ่งกฎกว้างจับได้มากกว่ากฎแคบ = มีคู่ที่ *เด็กเห็นเหมือนกันแต่ไบต์ต่างกัน*
     ⇒ นั่นคือของจริงที่ต้องยกให้ E ตัดสินว่าจะยกระดับเป็นคำตัดสินไหม ⛔ ยังไม่ตัดสินตอนนี้

**🔑 ด่านนี้พิมพ์รายการหมุด "ในรอบที่เขียวด้วย" ⛔ ไม่ใช่เฉพาะรอบที่แดง**

  เงื่อนไขของ E→MB #61 §2 — และเหตุผลของมัน:
     ถ้ารายชื่อโผล่เฉพาะตอนแดง ⇒ มันคือ *หนี้ที่มองเห็นได้* ⛔ ไม่ใช่ *หนี้ที่มีรายชื่อ*
     ⇒ ⇒ คำว่า "เขียว" ของด่านนี้จึงแปลว่า
        **"⛔ ไม่มีของซ้ำ นอกเหนือจากรายการที่พิมพ์อยู่ตรงนี้"** — ซึ่งเป็นความจริง
        ⛔ ไม่ใช่ ㊽ (㊽ คือการย่อที่ *ซ่อน* · อันนี้ย่อแล้ว *แสดง*)

**🔴 จุดบอดเชิงโครงสร้างของด่านนี้ — ประกาศเอง ⛔ ไม่รอให้คนอื่นมาค้นพบ (v1.1)**

  ด่านนี้เห็นเฉพาะตัวเลือกที่ **มีอยู่ในไฟล์**
  🔴 **ตัวเลือกที่หายไปตั้งแต่การถอดความจาก PDF — ด่านนี้ (ซึ่งอ่าน JSON อย่างเดียว) มองไม่เห็น**
     ⚠️ v1.3 · **ถ้อยคำเดิมของ v1.1–v1.2 ผิด และถูกถอนแล้ว** (E→MB #65 §4 · E ถอนคำของตัวเอง)
        เดิมเขียนว่า *ตาบอด 548 ข้อ* และ *มองไม่เห็นโดยหลักการ* ⇒ ⛔ ไม่จริงทั้งคู่
        ของจริง: **ต้นฉบับ PDF อ่านได้ ด้วยการเรนเดอร์หน้าเป็นภาพ + ตาคน**
        ⇒ ⇒ **ราคาคือ "ต่อหน้า" ⛔ ไม่ใช่ "อ่านไม่ได้"** — เป็นงานที่วางแผนได้ ⛔ ไม่ใช่กำแพง
     ⬤ MB ยืนยันเองแล้ว 10 ส.ค. 69 ⛔ ไม่ได้เชื่อคำบอกของ E (㊴):
        เรนเดอร์ '13 แคลคูลัส โจทย์.pdf' หน้า 26 และ '12 หลักการนับ…โจทย์.pdf' หน้า 12 ที่ 400 dpi
        ⇒ อ่านไทยออกครบทุกตัวอักษร ⇒ ได้ทั้งตัวเลือกของ q135 และตัวเลือกที่ 5 ที่หายของ q75
     🔴 **และราคายังแพงกว่าที่ E เขียนไว้ สำหรับไฟล์เฉลย** — MB วัดเอง 10 ส.ค. 69:
          '13 แคลคูลัส โจทย์.pdf'  ชั้นข้อความ 240,282 ตัวอักษร  ⇒ ค้นหน้าด้วยเลขข้อได้
          '13 แคลคูลัส เฉลย.pdf'   ชั้นข้อความ **0 ตัวอักษร**    ⇒ ⛔ ค้นไม่ได้เลย ต้องไล่ดูทีละหน้า
        ⇒ ⇒ ไฟล์ 'โจทย์' จ่ายค่า **อ่าน** ต่อหน้า · ไฟล์ 'เฉลย' จ่ายค่า **ค้น** ด้วย
     ⇒ พบแล้ว **9 ข้อ** ที่ข้อสอบจริงมี 5 ตัวเลือกแต่คลังเก็บ 4 (E→MB #63 §3 · E เปิด PDF อ่านเอง):
       chap-02-logic-q92 · chap-04-coord-q42
       chap-11-sequence-q22 · chap-11-sequence-q64
       chap-12-probability-q75
       chap-15-statistics-q04 · chap-15-statistics-q33
       chap-15-statistics-q46 · chap-15-statistics-q183
     ⚠️ v1.3 · **เขียนรหัสเต็มทั้ง 9 ชื่อ** — v1.1–v1.2 ย่อสี่ตัวท้ายเป็น "q33 · q46 · q183"
        ⇒ ㊽ ตรง ๆ: บรรทัดที่ย่อ **หน้าตาเหมือนบรรทัดที่ครบ** และ selftest ⑲ ก็อ่านไม่ออก
     ⬤ MB ยืนยันฝั่งคลังแล้ว: ทั้ง 9 ข้อมี 4 ตัวเลือกจริง และเสิร์ฟเด็กอยู่ทั้งหมด
     ⬤ และคลังมีข้อที่ใช้ 5 ตัวเลือกอยู่แล้ว **938 ข้อ** ⇒ 5 ตัวเลือกเป็นรูปปกติของคลังนี้
        ⇒ การมี 4 จึงเป็น **การสูญหาย** ⛔ ไม่ใช่ธรรมเนียมของชุด

  🔑 **ต้นเหตุเดียวกับของที่ด่านนี้จับได้ — คนละอาการ:**
       สแกนเพี้ยน → ตัวเลือก **หายไปหนึ่ง**      ⇒ 9 ข้อข้างบน ⇒ ⛔ ด่านนี้มองไม่เห็น
       สแกนเพี้ยน → ตัวเลือก **ถูกถอดซ้ำอีกตัว** ⇒ q135 · q194 ⇒ ✅ ด่านนี้เห็น

  ⇒ ⇒ **"เขียว" ของด่านนี้ ⛔ ไม่ใช่หลักฐานว่าตัวเลือกครบตามต้นฉบับ**
       เป็นหลักฐานว่า **ไม่มีตัวเลือกที่ซ้ำกันในไฟล์** เท่านั้น
  ⇒ การจับตระกูล "หายไปหนึ่ง" ต้องเทียบกับ PDF ต้นฉบับ ⛔ ไม่มีทางทำจากไฟล์ JSON อย่างเดียว

  🆕 v1.3 · **ของ 1 ใน 9 ข้อ อ่านออกแล้ว แต่ยังไม่คืน — และนั่นเป็นคำสั่งครู ⛔ ไม่ใช่ข้อจำกัด**
       chap-12-probability-q75 · ตัวเลือกที่ 5 ของต้นฉบับ (บท 12 หน้า 12 · MB อ่านเอง 400 dpi):
         "ถ้า (n+1)!/(n-1)! = 1,640 แล้ว n มีค่าเท่ากับ 41"
       ⬤ ตรงกับที่ field notes ของข้อนั้นบันทึกไว้เป๊ะทุกใจความ ⇒ คนถอดความ **เห็นมัน** แล้วเลือกไม่เก็บ
       ⬤ ข้อความนี้เป็นเท็จ ((n+1)n = 1,640 ⇒ n = 40) ⇒ คืนแล้ว **เฉลยไม่ขยับ** (ยังเป็นตัวเลือก 3)
       ⛔ **ยังไม่คืนในรอบนี้** — ครู QC เคาะ 10 ส.ค. 69 ว่าให้ส่งเรื่องให้ E ก่อน
       ⇒ บรรทัดนี้อยู่ที่นี่เพื่อให้ "ยังไม่คืน" เป็น **หนี้ที่มีชื่อ** ⛔ ไม่ใช่ของที่ค้างเงียบ ๆ

**⛔ รายการ `KNOWN_DUPES` ถูกด่าน 10 เฝ้าอยู่**
  ลงทะเบียนไว้ใน `WATCHED` ของ `scripts/check_config_growth.py` แล้วจริง
  ⇒ เติมชื่อที่สามเข้ามาเงียบ ๆ ⛔ ไม่ได้ — ต้องประกาศในข้อความคอมมิตว่า
     `KNOWN_DUPES+: <เหตุผล>`

**🔴 บทเรียน v1.3 — selftest ที่ยืมหนี้จริงมาเป็นวัตถุทดสอบ จะแดงในวันที่หนี้ถูกชำระ**

  วัดเอง 10 ส.ค. 69 (⛔ ไม่ใช่การคาดเดา): จำลองการซ่อม q135 แล้วรัน `--selftest` ของ v1.2
  ⇒ **ล้ม 5 เคส** ทั้งที่การรันกับข้อมูลจริง **เขียว** (รหัสออก 0)
      ⑫ ⑬ ⑭ ⑮ ล้มเพราะสร้างมิวแทนต์ด้วย `{k: dict(v, …) for k, v in KNOWN_DUPES.items()}`
              ⇒ หมุดหมดรายการ ⇒ มิวแทนต์กลายเป็น `{}` ⇒ ⛔ ไม่มีอะไรให้แดง ⇒ เคส "ต้องแดง" เขียว
      ⑲    ล้มเพราะตรึง **ถ้อยคำ** ของคำประกาศ ('มองไม่เห็นโดยโครงสร้าง') ไว้เป็นเงื่อนไขผ่าน
              ⇒ วันที่คำประกาศนั้นถูกพิสูจน์ว่าผิด **การแก้ให้ถูกทำให้ด่านแดง**

  ⇒ ⇒ **ยาแก้ v1.3 (สองขนาน — ⛔ ไม่ใช่ขนานเดียว จึงอาจเป็นคนละตระกูล)**
      ① มิวแทนต์ต้องมี **ฟิกซ์เจอร์ของตัวเอง** (`FIX_*` ข้างล่าง) ⛔ ห้ามยืมรายการจริง
         · และมี ㉓ เฝ้าไม่ให้ลวดลายเดิมกลับมา (⑦ก: คำสั่งที่ไม่มีด่านเฝ้า = คอมเมนต์)
      ② เคสที่ตรวจหัวไฟล์ต้องยืนบน **ข้อมูล** (รายชื่อ 9 รหัสข้อ) ⛔ ไม่ใช่บนวลี
         · ⛔ และห้ามใช้ "ห้ามมีวลีนี้" เป็นเงื่อนไข เพราะกฎ "ห้ามลบบันทึกเก่า" บังคับให้วลีที่ถอนแล้ว
           ต้องคงอยู่ในไฟล์ ⇒ เงื่อนไขแบบนั้นจะแดงตลอดกาล (⑯: ด่านที่แดงเสมอ = ด่านที่ถูกปิด)

  ⚠️ **ขอบเขตที่ยังเปิดอยู่:** ยานี้แก้ที่ด่าน 15 ใบเดียว — ⛔ **ยังไม่ได้กวาดด่านอีก 19 ใบ**
     ว่ามี selftest ใบไหนยืมข้อมูลจริงหรือตรึงถ้อยคำแบบเดียวกันอีก (ยกให้ E ให้เลขและตัดสินขอบเขต)

**🔴 บทเรียน v1.4 — E ทำนายว่ายาแก้ v1.3 ยังป่วย · ผมทดสอบแล้ว **หักล้างได้** · แต่เขาชี้ไปโดนรูจริงที่อยู่ข้าง ๆ**

  **คำทำนาย (E→MB #67 §4):** *"วันที่ครูเคาะให้คืนตัวเลือกที่ 5 ของ q75 ⇒ ข้อนั้นออกจากรายการ
  ⇒ เหลือ 8 ⇒ ⑲ จะแดงในวันที่งานสำเร็จ เหมือน ⑫–⑮ เป๊ะ"* — และ E ติดป้ายเองว่า
  *"อ่านจากคำบรรยาย ⛔ ยังไม่ได้เปิดโค้ด ⇒ นี่คือคำทำนายที่หักล้างได้"*

  ⬤ **ผมรันจริง 10 ส.ค. 69** (⛔ ไม่ตอบจากการอ่านโค้ด — จำลองการคืนตัวเลือกแล้วรันตัวจริง):
     คืน choices[4] ให้ chap-12-probability-q75 ⇒ 5 ตัวเลือก · correct = 2 เท่าเดิม
     ⇒ `--selftest` : **ผ่านครบ 28/28** · รันกับข้อมูลจริง : **เขียว รหัสออก 0**
  ⇒ ⇒ **คำทำนายหักล้างแล้ว** — เพราะรายการนี้เป็น *ลิสต์ตายตัวในซอร์ส* ⛔ ไม่ได้คำนวณจากข้อมูล
       ⇒ การคืนตัวเลือกในคลัง ⛔ ไม่แตะมันเลย

  > 🔑 **แต่สัญชาตญาณของ E ชี้ไปถูกที่ — แค่ผิดกลไก · และของจริงแย่กว่า:**

  ⬤ **มิวแทนต์ที่ผมยิงใส่ตัวเอง (⛔ ไม่ใช่การคาดเดา):**
       ย่อรายการจาก **9 ชื่อ เหลือ 1 ชื่อ** ⇒ `--selftest` **ผ่านครบ 28/28**
       ย่อ *หัวไฟล์* ตามไปด้วยอีก 8 ชื่อ ⇒ **ผ่านอีก**
  ⇒ ⇒ 🔴 **⑲ ของ v1.3 เฝ้า "ความสอดคล้องภายในไฟล์" ⛔ ไม่ได้เฝ้า "ความครบ"**
       สองข้างที่มันเทียบ (`BLIND_CENSUS` กับ `__doc__`) อยู่ใน **ไฟล์เดียวกัน**
       ⇒ ⇒ **แก้พร้อมกันก็ผ่าน** ⇒ คำประกาศจุดบอดหดได้เงียบสนิท

  ### 🔑 **กฎที่ถอดออกมา — ใช้กับด่านอื่นได้ทันที**
  > **"เคสที่เทียบไฟล์กับตัวเอง เฝ้าได้แค่ *ความสอดคล้อง* ⛔ ไม่มีวันเฝ้า *ความครบ*
  >   ⇒ ความครบต้องมีหลักยึด **นอกไฟล์** — และในโครงการนี้ หลักยึดนั้นคือ `git`"**

  ✅ **ยาแก้ v1.4:** ลงทะเบียน `BLIND_CENSUS` ใน **`FROZEN`** ของด่าน 10 v1.2 (ทะเบียนใหม่)
     · `WATCHED` เฝ้า **ขาขึ้น** อย่างเดียว — "ลดสิทธิ์ไม่ต้องขออนุญาต"
     · `FROZEN` เฝ้า **สองขา** — เพราะสำมะโน ⛔ ไม่ใช่สิทธิ์ · **ลบชื่อ = ลบหลักฐาน**
     · ย่อรายการ ⇒ ต้องประกาศ `BLIND_CENSUS-: <เหตุผล>` ในข้อความคอมมิต
     · ⑲ก–⑲ง เฝ้าว่ายานี้ยังต่ออยู่จริง — รวมเคสที่ **เรียก `frozen_verdict` ตัวจริงมารัน**

  ⚠️ **และวิธีที่ถูกต้องเมื่อข้อใดถูกซ่อม: ทำเครื่องหมาย ✅ ในหัวไฟล์ ⛔ ไม่ใช่ลบชื่อออก**
     ⇒ "เคยขาด" เป็นข้อเท็จจริงที่ ⛔ ไม่มีวันเป็นเท็จ ⇒ รายการนี้จึง ⛔ ไม่มีวันหด
     ⇒ ⇒ **นั่นคือเหตุผลที่มันแดงตอนหด ⛔ ไม่ใช่ความเข้มงวดเกินเหตุ**

**วิธีรัน**

    python3 scripts/check_duplicate_choices.py             # ตรวจของจริง (data/sets)
    python3 scripts/check_duplicate_choices.py --selftest  # ตรวจตัวด่านเอง (ดี/เสีย/มิวแทนต์)

รหัสออก: 0 = ผ่าน · 1 = เนื้อหาแดง · 2 = ตัวเครื่องมือแดง
"""
from __future__ import annotations

import argparse
import collections
import json
import re
import sys
from pathlib import Path

CHECKER_VERSION = '1.4'
CHECKER_DATE = '2026-08-10'

ROOT = Path(__file__).resolve().parent.parent
DEFAULT_SCAN = ROOT / 'data' / 'sets'

# เหตุผลสั้นกว่านี้ = คนเขียนยังไม่ได้คิด (ทรงเดียวกับ MIN_REASON ของด่าน 14)
MIN_REASON = 40

# ── 🔑 หมุดของเก่า — ปักเป็น "รายชื่อ + คู่ดัชนี + ข้อความ" ⛔ ไม่ใช่ตัวเลข ────────
#
# ⛔ ห้ามเติมชื่อใหม่ลงรายการนี้เพื่อให้ด่านเขียว — รายการนี้ **ย่อได้อย่างเดียว**
# ⛔ ทั้งสองข้อเป็น **ข้อสอบจริง** ⇒ ทางออกคือธง `flawedSource` ⛔ ไม่ใช่แก้ตัวเลือก
#    (คำสั่งผูกพัน: ห้ามแตะโจทย์/ตัวเลือกของข้อสอบจริง) ⇒ **ครูเป็นคนเคาะ**
# 🔴 v1.2 · **รายการแบนที่ด่าน 10 อ่านได้จริง** — ⛔ ไม่ใช่ของแถม ⛔ ไม่ใช่ของซ้ำซ้อน
#
#   ที่มา: v1.0 ลงทะเบียน `KNOWN_DUPES` ไว้ใน WATCHED ของด่าน 10 แล้ว **แต่ด่าน 10 อ่านมันไม่ได้เลย**
#     ① `parse_list` จับด้วย regex `^\s*VAR\s*=\s*…` ⇒ `KNOWN_DUPES: dict[...] = {` **มีป้ายชนิดคั่น** ⇒ ไม่ตรง
#     ② ถึงถอดป้ายชนิดออก ค่าก็เป็น dict **ซ้อนชั้น** ⇒ regex `\{[^}]*\}` หยุดที่ `}` ตัวใน ⇒ อ่านไม่ออกอยู่ดี
#   🔴 และรอบแรกมัน **ไม่แดง** เพราะไฟล์ยังไม่เคยมีในประวัติ ⇒ ด่าน 10 ตอบว่า "ยังไม่มีของให้เทียบ"
#      ⇒ ⇒ **คำตอบที่ซื่อสัตย์ กลบข้อเท็จจริงว่ามันจะเทียบไม่ได้ตลอดกาล** — โผล่จริงตอนคอมมิตที่สอง
#
#   ⇒ ทางแก้: ให้ด่าน 10 เฝ้า **รายการรหัสข้อแบน ๆ** ตัวนี้ (อ่านออกแน่นอน)
#      ส่วนรายละเอียด (คู่ดัชนี · ข้อความ · เหตุ · หมดอายุ) อยู่ใน KNOWN_DUPES ตามเดิม
#      และ selftest ⑱ **เรียก parse_list ของด่าน 10 มารันจริงกับไฟล์นี้** ⛔ ไม่ใช่แค่เช็กว่ามีชื่อใน WATCHED
#   ⛔ สองรายการต้องตรงกันเสมอ — มี selftest ㉒ บังคับ (ไม่ตรง = แดง)
KNOWN_DUPE_IDS = []

KNOWN_DUPES: dict[str, dict] = {}

# ── 🧬 ฟิกซ์เจอร์สังเคราะห์ของ selftest — v1.3 ─────────────────────────────
#
# 🔑 **เหตุผลที่มันต้องมีอยู่ ⛔ แม้รายการหมุดจะว่าง** (บทเรียน v1.3 · ดูหัวไฟล์)
#    มิวแทนต์ของ v1.2 สร้างจาก `KNOWN_DUPES` ตัวจริง ⇒ วันที่หมุดตัวสุดท้ายถูกถอน
#    (= วันที่งานสำเร็จ) มิวแทนต์กลายเป็น `{}` ⇒ เคส "ต้องแดง" เขียวเงียบ ๆ 4 เคส
#    ⇒ ⇒ **ชุดทดสอบต้องเป็นเจ้าของวัตถุทดสอบของตัวเอง** ⛔ ไม่ยืมสภาพปัจจุบันของคลัง
# ⛔ รหัสข้อนี้ขึ้นต้น 'chap-00-' ซึ่ง ⛔ ไม่มีอยู่จริงในคลัง ⇒ ⛔ ไม่มีวันชนของจริง
# 🔴 **สำมะโน "ข้อที่ต้นฉบับมี 5 ตัวเลือกแต่คลังเก็บ 4" — พบ 9 ข้อ เมื่อ 10 ส.ค. 2569**
#
# ⛔ **นี่คือ "ข้อเท็จจริงที่มีวันที่" ⛔ ไม่ใช่ "รายการที่ยังขาดอยู่ตอนนี้"**
#    ⇒ วันที่ข้อไหนถูกซ่อม **ให้ทำเครื่องหมาย ✅ ในหัวไฟล์ ⛔ ไม่ใช่ลบชื่อออกจากรายการนี้**
#    ⇒ ⇒ เพราะ "เคยขาด" เป็นข้อเท็จจริงที่ ⛔ ไม่มีวันเป็นเท็จ (กฎห้ามลบบันทึกเก่า)
#    ⇒ ⇒ ⇒ และเพราะรายการที่ *ย่อได้* จะแดงในวันที่งานสำเร็จ — ซึ่งคือโรคที่ v1.3 เพิ่งรักษา
#
# 🔴 **ทำไมเปลี่ยนชื่อจาก `BLIND_9` (v1.3) — เลขในชื่อคือค่าที่อยู่ในที่ที่อยู่นานกว่าค่า (㊿)**
#
# 🔴 **และทำไม selftest ⑲ ของ v1.3 ⛔ ไม่พอ — MB วัดจริง 10 ส.ค. 69:**
#      ย่อรายการจาก 9 ชื่อเหลือ 1 ชื่อ ⇒ selftest **ผ่านครบ 28/28**
#      ย่อหัวไฟล์ตามไปด้วย            ⇒ **ผ่านอีก**
#    ⇒ ⇒ ⑲ เฝ้า **"ความสอดคล้องภายในไฟล์"** ⛔ ไม่ได้เฝ้า **"ความครบ"**
#         สองข้างที่มันเทียบอยู่ในไฟล์เดียวกัน ⇒ **แก้พร้อมกันก็ผ่าน**
#    ⇒ ⇒ ⇒ ยาแก้อยู่ **นอกไฟล์**: ลงทะเบียนใน `FROZEN` ของด่าน 10 (v1.2)
#            ซึ่งเทียบกับ **คอมมิตฐานใน git** ⇒ ย่อรายการ ⇒ ต้องประกาศ `BLIND_CENSUS-:`
#
# ── 🔴 13 ส.ค. 69 · ทำเครื่องหมายเท็จบวก 3 ชื่อ (E→MB #71 §5② · ถ้อยคำจาก MB→E #72 §6) ──
#
# 🔴 3 ชื่อข้างล่างนี้ = **เท็จบวก — สำมะโนจับคู่ผิดข้อ (ตรวจต้นฉบับแล้ว 11 ส.ค. 69)**
#    chap-02-logic-q92     → ของจริงคือ PDF ข้อ 93 (มี 4 ตัวเลือก)
#    chap-11-sequence-q22  → PDF ข้อ 29  (มี 4)
#    chap-11-sequence-q64  → PDF ข้อ 143 (มี 4)
#    ⛔ **ห้ามลบชื่อออก** — "เคยถูกสงสัย" เป็นข้อเท็จจริงที่ ⛔ ไม่มีวันเป็นเท็จ (กติกา FROZEN)
#    ⛔ **และ ⛔ ไม่ใช้ ✅** — E ชี้ถูกว่า ✅ เป็นป้ายที่โกหก เพราะ 3 ข้อนั้น ⛔ ไม่เคยพัง
#       (ป้าย ✅ แปลว่า "ซ่อมแล้ว" · ของจริงคือ "ไม่เคยเสีย" — คนละเรื่อง · ㉟)
#
# 🔴🔴 **สำมะโนนี้ผลิตจากการจับคู่ด้วย *เลขข้อ*** — และเลขข้อของคลังกับต้นฉบับ ⛔ ไม่ตรงกันทุกบท
#    บท 12 · บท 13 ตรงพอดี · บท 02 เลื่อน −1 คงที่ · บท 11 เลื่อนไม่คงที่ (−7 และ −79)
#    ⇒ ⇒ **รายการนี้เป็น "พื้น" ⛔ ไม่ใช่ "จำนวน"** — เท็จลบในบท 02/11 ⛔ ยังไม่มีใครวัด
#    ⇒ เงื่อนไขที่จะทำให้บรรทัดนี้ถูกถอน: อ่านต้นฉบับบท 02 (27 หน้า) + บท 11 (29 หน้า) ครบ
#       แล้วจับคู่ด้วย **เนื้อหา** ⛔ ไม่ใช่ด้วยเลข  (〔52〕)
BLIND_CENSUS = [
    'chap-02-logic-q92', 'chap-04-coord-q42',
    'chap-11-sequence-q22', 'chap-11-sequence-q64',
    'chap-12-probability-q75',
    'chap-15-statistics-q04', 'chap-15-statistics-q33',
    'chap-15-statistics-q46', 'chap-15-statistics-q183',
]

FIX_ID = 'chap-00-fixture-q01'
FIX_TEXT = 'ตัวเลือกฝาแฝดสังเคราะห์ (⛔ ไม่ใช่ข้อจริง)'
FIX_EXACT = {FIX_ID: [(1, 3, FIX_TEXT)]}
FIX_IDS = [FIX_ID]
FIX_PINS = {
    FIX_ID: {
        'pair': (1, 3),
        'text': FIX_TEXT,
        'why': ('หมุดสังเคราะห์สำหรับชุดทดสอบเท่านั้น — ⛔ ไม่เกี่ยวกับข้อจริงข้อใด '
                'และยาวเกิน MIN_REASON โดยตั้งใจ เพื่อให้มิวแทนต์ "เหตุผลสั้น" มีของให้ทำลาย'),
        'until': 'ไม่มีวันหมด — ฟิกซ์เจอร์ ⛔ ไม่ใช่ข้อยกเว้นของคลัง',
    },
}

# ── 🗂️ ประวัติหมุดที่ **ถอนแล้ว** — ⛔ ห้ามลบบล็อกนี้ (คำสั่งผูกพัน "ห้ามลบบันทึกเก่า")
#    ⛔ บล็อกนี้เป็น **คอมเมนต์** ⇒ ⛔ ไม่มีน้ำหนักในการตัดสิน ⇒ ⛔ ไม่ใช่รายการยกเว้น
#
#  10 ส.ค. 69 (รอบบ่าย) · ถอน 'chap-13-calculus-q135'  [1]=[3] = "$6$ เมตร/วินาที, $\\dfrac{44}{3}$ เมตร"
#     ⬤ **หมุดตัวสุดท้ายของด่านนี้** ⇒ รายการว่างตั้งแต่คอมมิตนี้
#     เหตุที่ถอน: **ของจริงถูกซ่อมแล้ว** ⛔ ไม่ใช่ยกเลิกข้อยกเว้น
#        ตัวเลือก [1] ถูกถอดความผิดจาก PDF ⇒ คืนเป็น "$6$ เมตร/วินาที, $8$ เมตร" ในคอมมิตเดียวกัน
#        ขาที่ 1 : E อ่าน text layer ของ '13 แคลคูลัส โจทย์.pdf'            (E→MB #63)
#        ขาที่ 2 : E เรนเดอร์หน้าเดียวกันเป็นภาพแล้วอ่านด้วยตา              (E→MB #65 §3)
#        ขาที่ 3 : โครงสร้างตัวลวง — 44/3 คือค่าที่ได้เมื่อลืม s(0)=4/3 ⇒ เป็นตัวลวงของ [3] อยู่แล้ว
#        ขาที่ 4 : **MB เรนเดอร์หน้า 26 ที่ 400 dpi แล้วอ่านเอง** ⛔ ไม่ผ่านคำบอกของ E (㊴)
#                 md5 ของ PDF ที่อ่าน = e3fca600f5f6884accde10e60a065754
#     ⚠️ นี่คือครั้งที่ **สอง** ที่โครงการแก้ข้อความตัวเลือกของข้อสอบจริง ⇒ ครู QC เคาะจาก diff
#     ⬤ บันทึกฉบับเต็มอยู่ใน notes ของข้อนั้นเอง
#
#  10 ส.ค. 69 · ถอน 'chap-13-calculus-q194'  [1]=[3] = "$x^{2}\\cos 2x+2x\\sin 2x$"
#     เหตุที่ถอน: **ของจริงถูกซ่อมแล้ว** ⛔ ไม่ใช่ยกเลิกข้อยกเว้น
#        ตัวเลือก [3] ถูกถอดความผิดจาก PDF ⇒ คืนเป็น "x²cos2x + x·sin2x" ในคอมมิตเดียวกัน
#        ขาที่ 1 : E เปิด '13 แคลคูลัส โจทย์.pdf' ข้อ 194 อ่านด้วยตา (E→MB #63 §4)
#        ขาที่ 2 : MB ตรวจโครงสร้างตาราง 2×2 ของตัวลวง — ช่องที่หายคือ (1,1) ช่องเดียว
#     ⇒ กฎ ② ของด่านนี้บังคับให้ถอน: หมุดที่ไม่มีของรองรับแล้ว ⇒ แดง
#        ⇒ **การซ่อมข้อมูลกับการถอนหมุดต้องอยู่คอมมิตเดียวกันเสมอ**
#     ⬤ บันทึกฉบับเต็มอยู่ใน notes ของข้อนั้นเอง (ที่ที่มันจะไม่หายไปกับไฟล์นี้)


# ─────────────────────────────────────────────────────────────
# ชั้นกวาด
# ─────────────────────────────────────────────────────────────
def dup_pairs(choices):
    """คืนลิสต์ (i, j, ข้อความ) ของคู่ที่ซ้ำกันทุกไบต์ — เรียงแล้ว ⛔ ไม่ซ้ำคู่"""
    seen, out = {}, []
    for i, c in enumerate(choices):
        if c in seen:
            out.append((seen[c], i, c))
        else:
            seen[c] = i
    return out


def _norm_loose(s):
    """กฎกว้าง — ⛔ ไม่ใช่ตัวตัดสิน ใช้เป็นตัวเฝ้าความสอดคล้องเท่านั้น"""
    s = re.sub(r'\s+', '', s)
    s = s.replace('\\dfrac', '\\frac').replace('\\tfrac', '\\frac')
    s = s.replace('\\left', '').replace('\\right', '').replace('$', '')
    return s


def scan(paths):
    """คืน (จำนวนข้อที่มีตัวเลือก, {qid: [(i,j,text)]}, {qid: True} ของกฎกว้าง)"""
    divisor, exact, loose = 0, {}, {}
    for p in sorted(paths):
        try:
            d = json.loads(Path(p).read_text(encoding='utf-8'))
        except Exception as e:
            raise SystemExit(f'  🔴 อ่าน {p} ไม่ได้: {e}')
        for q in d.get('questions') or []:
            ch = q.get('choices')
            if not isinstance(ch, list) or len(ch) < 2:
                continue
            divisor += 1
            qid = q.get('id') or '(ไม่มี id)'
            dp = dup_pairs(ch)
            if dp:
                exact[qid] = dp
            c = collections.Counter(_norm_loose(x) for x in ch)
            if max(c.values()) > 1:
                loose[qid] = True
    return divisor, exact, loose


# ─────────────────────────────────────────────────────────────
# คำตัดสิน — ⛔ ไม่พิมพ์เอง ⛔ ไม่ exit เอง (แยกจาก report เพื่อให้ selftest เรียกได้)
# ─────────────────────────────────────────────────────────────
def verdict(divisor, exact, pins, ids=None):
    """คืน (ลิสต์ข้อความแดง, ลิสต์บรรทัดรายการหมุดที่ต้องพิมพ์ทุกรอบ)"""
    bad = []

    # v1.2 · รายการแบนที่ด่าน 10 เฝ้า ต้องตรงกับรายการจริงเสมอ
    #   ⛔ ถ้าไม่บังคับ คนจะเติมหมุดใน KNOWN_DUPES โดยไม่แตะรายการแบน
    #      ⇒ ด่าน 10 เห็นรายการยาวเท่าเดิม ⇒ **การขยายสิทธิ์ผ่านไปเงียบ ๆ**
    ids = KNOWN_DUPE_IDS if ids is None else ids
    if set(ids) != set(pins):
        bad.append('KNOWN_DUPE_IDS ⛔ ไม่ตรงกับ KNOWN_DUPES — '
                   'มีในรายการแบนแต่ไม่มีหมุด: %s · มีหมุดแต่ไม่อยู่ในรายการแบน: %s '
                   '⇒ ด่าน 10 จะเฝ้าของผิดตัว'
                   % (sorted(set(ids) - set(pins)) or '(ไม่มี)',
                      sorted(set(pins) - set(ids)) or '(ไม่มี)'))

    # ④ ตัวหารเป็นศูนย์ = ⛔ ไม่ได้ตรวจอะไร ⇒ ห้ามเขียว
    if divisor == 0:
        bad.append('ตัวหารเป็นศูนย์ — ⛔ ไม่พบข้อชนิดมีตัวเลือกเลย '
                   '⇒ "เขียวเพราะไม่ได้ตรวจ" ต้องไม่หน้าตาเหมือน "เขียวเพราะสะอาด"')

    # ③ เหตุผลของหมุดต้องยาวพอ + มีคีย์ครบ
    for qid, pin in sorted(pins.items()):
        for k in ('pair', 'text', 'why', 'until'):
            if k not in pin:
                bad.append(f'หมุด {qid} ขาดคีย์ "{k}"')
        if len((pin.get('why') or '').strip()) < MIN_REASON:
            bad.append(f'หมุด {qid} เหตุผลสั้นกว่า {MIN_REASON} ตัวอักษร '
                       f'(เหตุผลเปล่า = การเลี่ยงกฎ)')
        if not (pin.get('until') or '').strip():
            bad.append(f'หมุด {qid} ⛔ ไม่มีเงื่อนไขหมดอายุ ("until") '
                       f'⇒ หมุดที่ไม่มีวันหมดอายุ = ข้อยกเว้นถาวรที่ไม่มีใครทบทวน')

    # ① ของซ้ำที่ ⛔ ไม่ได้ปักหมุด ⇒ แดง
    for qid, dps in sorted(exact.items()):
        pin = pins.get(qid)
        if pin is None:
            for i, j, t in dps:
                bad.append(f'🆕 {qid} ตัวเลือก [{i}] กับ [{j}] ซ้ำกันทุกไบต์ '
                           f'⇒ ⛔ ไม่ได้ปักหมุดไว้ : {t[:70]}')
            continue
        # ③ หมุดต้องตรงกับของจริงเป๊ะ (ทรงเดียวกับตาราง SHAPE ของด่าน 20)
        real = {(i, j) for i, j, _ in dps}
        if tuple(pin['pair']) not in real:
            bad.append(f'หมุด {qid} ระบุคู่ {tuple(pin["pair"])} '
                       f'แต่ของจริงซ้ำที่ {sorted(real)} ⇒ หมุดกับของจริงไม่ตรงกัน')
        else:
            t = next(t for i, j, t in dps if (i, j) == tuple(pin['pair']))
            if t != pin['text']:
                bad.append(f'หมุด {qid} ข้อความในหมุดไม่ตรงกับของจริง '
                           f'⇒ ตัวเลือกถูกแก้แล้วแต่หมุดยังเป็นของเดิม')
        # ถ้าซ้ำหลายคู่ แต่หมุดครอบแค่คู่เดียว ⇒ คู่ที่เหลือต้องแดง
        for i, j, t in dps:
            if (i, j) != tuple(pin['pair']):
                bad.append(f'🆕 {qid} ตัวเลือก [{i}] กับ [{j}] ซ้ำกันเพิ่มอีกคู่ '
                           f'⇒ หมุดครอบแค่คู่ {tuple(pin["pair"])} : {t[:60]}')

    # ② หมุดที่ไม่มีของรองรับแล้ว ⇒ แดง (รายการย่อได้อย่างเดียว)
    for qid in sorted(pins):
        if qid not in exact:
            bad.append(f'หมุด {qid} ⛔ ไม่มีของรองรับแล้ว — ข้อนี้ไม่มีตัวเลือกซ้ำ '
                       f'(หรือข้อหายไป) ⇒ **ต้องถอนหมุดออกจาก KNOWN_DUPES**')

    lines = []
    for qid, pin in sorted(pins.items()):
        i, j = pin.get('pair', ('?', '?'))
        lines.append(f'    {qid}  [{i}]=[{j}]  {(pin.get("text") or "")[:56]}')
        lines.append(f'        เหตุ    : {pin.get("why", "")}')
        lines.append(f'        หมดอายุ : {pin.get("until", "")}')
    return bad, lines


def report(paths, quiet=False, pins=None):
    pins = KNOWN_DUPES if pins is None else pins
    divisor, exact, loose = scan(paths)
    bad, pinlines = verdict(divisor, exact, pins)

    if not quiet:
        print()
        print('▼▼▼ ด่าน 15 · ตัวเลือกในข้อเดียวกันต้องไม่ซ้ำกัน '
              f'(v{CHECKER_VERSION}) ▼▼▼')
        # ㊳ ห้าขา — ที่มา · เวลา(รันสด) · หน่วย · ขอบเขต
        print(f'  ขอบเขตที่นับ : {len(list(paths))} ไฟล์ · '
              f'ข้อชนิดมีตัวเลือก (choices ≥ 2) **{divisor} ข้อ**')
        print(f'  พบตัวเลือกซ้ำทุกไบต์ : {len(exact)} ข้อ · '
              f'ปักหมุดไว้ {len(pins)} ข้อ')

        # 🔑 เงื่อนไข E→MB #61 §2 — พิมพ์รายการหมุด **ทุกรอบ รวมรอบที่เขียว**
        print()
        print('  ── 🪧 รายการหมุดที่ประกาศไว้ (พิมพ์ทุกรอบ รวมรอบที่เขียว) ──')
        if pinlines:
            for ln in pinlines:
                print(ln)
        else:
            print('    (ว่าง — ⛔ ไม่มีข้อยกเว้นใด ๆ)')

        # ตัวเฝ้าความสอดคล้อง — กฎกว้าง ⛔ ไม่ใช่คำตัดสิน
        extra = sorted(set(loose) - set(exact))
        print()
        if extra:
            print(f'  ⚠️ ตัวเฝ้าความสอดคล้อง: กฎกว้าง (ตัดช่องว่าง/dfrac/left-right/$) '
                  f'จับได้เพิ่ม {len(extra)} ข้อ')
            for qid in extra:
                print(f'       {qid}')
            print('     ⇒ คู่ที่ **เด็กเห็นเหมือนกันแต่ไบต์ต่างกัน** '
                  '⇒ ⛔ ยังไม่ตัดสิน — ยกให้ E เคาะว่าจะยกระดับเป็นคำตัดสินไหม')
        else:
            print('  ✅ ตัวเฝ้าความสอดคล้อง: กฎกว้างจับได้เท่ากับกฎแคบพอดี '
                  '(⛔ ไม่มีคู่ที่ต่างกันแค่รูปแบบ)')

        print()
        if bad:
            for m in bad:
                print('  ❌ ' + m)
            print(f'  🔴 ด่าน 15 แดง — {len(bad)} เรื่อง')
        else:
            print('  ✅ ⛔ ไม่มีตัวเลือกซ้ำ นอกเหนือจากรายการหมุดที่พิมพ์ไว้ข้างบน')
    return len(bad)


# ─────────────────────────────────────────────────────────────
# ชุดทดสอบตัวด่านเอง — ㊶ ต้องมีทั้งเคสจับได้และเคสปล่อย และมาจากข้อมูลจริง
# ─────────────────────────────────────────────────────────────
def selftest():
    ok = True
    n_case = 0

    def say(n, label, cond):
        # ⛔ v1.3 · นับเคสเอง — เดิมพิมพ์ "ผ่านครบ 25" ที่เขียนด้วยมือ
        #    ⇒ เลขที่ ⛔ ไม่ได้วัด และเพี้ยนเงียบ ๆ ทุกครั้งที่เพิ่ม/ลบเคส (㉚)
        nonlocal ok, n_case
        n_case += 1
        print(f'  {"✅" if cond else "❌"} {n} {label}')
        ok = ok and bool(cond)

    _SRC = Path(__file__).resolve().read_text(encoding='utf-8')
    # 🔑 v1.4 · โหลดด่าน 10 ตัวจริงตั้งแต่ต้น — เคส ⑲ข–⑲ง ต้องใช้ก่อนถึงบล็อก ④
    #    (⛔ ไม่อ่านเป็นข้อความอย่างเดียว — **เรียกฟังก์ชันจริงมารัน** ทรงเดียวกับ ⑱ข)
    cg = (ROOT / 'scripts' / 'check_config_growth.py').read_text(encoding='utf-8')
    import importlib.util as _ilu
    _sp = _ilu.spec_from_file_location('_cg10', ROOT / 'scripts' / 'check_config_growth.py')
    _m = _ilu.module_from_spec(_sp); _sp.loader.exec_module(_m)
    print(f'ชุดทดสอบด่าน 15 v{CHECKER_VERSION} ({CHECKER_DATE})')

    # ── ① ชั้นหาคู่ซ้ำ
    say('①', 'ไม่มีอะไรซ้ำ ⇒ คืนลิสต์ว่าง', dup_pairs(['a', 'b', 'c']) == [])
    say('②', 'ซ้ำคู่เดียว ⇒ คืน (i,j) ถูกตัว',
        dup_pairs(['a', 'b', 'c', 'b']) == [(1, 3, 'b')])
    say('③', 'ซ้ำสามตัว ⇒ ต้องได้สองคู่ ⛔ ไม่ใช่คู่เดียว',
        len(dup_pairs(['x', 'x', 'x'])) == 2)
    say('④', '(ต้องไม่จับ) ต่างกันแค่ช่องว่าง ⇒ กฎแคบต้องเงียบ',
        dup_pairs(['a b', 'ab']) == [])
    say('⑤', '…แต่กฎกว้างต้องเห็น (ไม่งั้นตัวเฝ้าความสอดคล้องไร้ความหมาย)',
        _norm_loose('a b') == _norm_loose('ab'))
    say('⑥', '(ต้องไม่จับ) ตัวเลือกเดียว ⇒ ⛔ ไม่นับเป็นข้อในตัวหาร',
        scan_stub([['a']])[0] == 0)

    # ── ② คำตัดสิน · ของจริง
    real = sorted(str(p) for p in DEFAULT_SCAN.glob('*.json'))
    divisor, exact, loose = scan(real)
    say('⑦', f'ตัวหารของจริงต้องไม่เป็นศูนย์ (วัดได้ {divisor})', divisor > 0)
    say('⑧', f'ของจริงพบซ้ำ {len(exact)} ข้อ · ปักหมุดไว้ {len(KNOWN_DUPES)} ข้อ ⇒ ต้องเขียว',
        verdict(divisor, exact, KNOWN_DUPES)[0] == [])
    say('⑨', 'หมุด 1 ตัวต้องให้ 3 บรรทัดพิมพ์เสมอ (เงื่อนไข E→MB #61: เขียวก็ต้องพิมพ์) '
             '· 🔑 v1.3 วัดด้วยฟิกซ์เจอร์ ⇒ ⛔ ไม่กลายเป็นเคสเปล่าตอนรายการหมุดว่าง',
        len(verdict(1, FIX_EXACT, FIX_PINS, ids=FIX_IDS)[1]) == 3
        and len(verdict(divisor, exact, KNOWN_DUPES)[1]) == 3 * len(KNOWN_DUPES))
    say('⑲', 'v1.3 · หัวไฟล์ต้องประกาศจุดบอดด้วย **รายชื่อรหัสข้อ** (E→MB #63 §3ข) '
             '⛔ ⛔ ไม่ตรึง "ถ้อยคำ" อีกแล้ว — v1.2 ตรึงวลีไว้ แล้ววลีนั้นถูกพิสูจน์ว่าผิด',
        all(x in __doc__ for x in BLIND_CENSUS))
    say('⑲ก', '🆕 v1.4 · สำมะโนต้อง ⛔ ไม่มีชื่อซ้ำ (ชื่อซ้ำ = ตัวหารเฟ้อโดยไม่มีใครเห็น)',
        len(set(BLIND_CENSUS)) == len(BLIND_CENSUS))
    say('⑲ข', '🔑 v1.4 · สำมะโนต้องลงทะเบียนใน **FROZEN** ของด่าน 10 ⛔ ไม่ใช่ WATCHED '
              '— WATCHED เฝ้าขาขึ้นอย่างเดียว ⇒ **ย่อรายการได้เงียบ ๆ** (MB ทำจริงแล้ว 9→1 ผ่าน 28/28)',
        "'scripts/check_duplicate_choices.py', 'BLIND_CENSUS'" in cg
        and cg.index('FROZEN = [') < cg.index("'scripts/check_duplicate_choices.py', 'BLIND_CENSUS'"))
    say('⑲ค', '🔑 v1.4 · ตัวอ่านของด่าน 10 ต้องอ่าน BLIND_CENSUS ออกจริง '
              '(ไม่งั้น FROZEN เฝ้าของที่มองไม่เห็น — ทรงเดียวกับรู absent ที่เพิ่งอุด)',
        _m.parse_list(_SRC, 'BLIND_CENSUS')[0] == 'ok'
        and sorted(_m.parse_list(_SRC, 'BLIND_CENSUS')[1] or []) == sorted(BLIND_CENSUS))
    say('⑲ง', '🧬 v1.4 · และด่าน 10 ต้องแดงจริงถ้าสำมะโนถูกย่อโดยไม่ประกาศ '
              '(⛔ ไม่ใช่แค่มีชื่ออยู่ในรายการ FROZEN)',
        any(c == 'undeclared-shrink' for c, _ in
            _m.frozen_verdict(BLIND_CENSUS, BLIND_CENSUS[:1], [''], 'BLIND_CENSUS'))
        and _m.frozen_verdict(
            BLIND_CENSUS, BLIND_CENSUS[:1],
            ['BLIND_CENSUS-: ซ่อมครบแล้วจึงถอนชื่อออกตามมติครู'], 'BLIND_CENSUS') == [])
    say('⑳', '🆕 v1.1 · ประวัติหมุดที่ถอนแล้วต้องยังอยู่ในไฟล์ (ห้ามลบบันทึกเก่า)',
        'chap-13-calculus-q194' in _SRC and 'ประวัติหมุดที่ **ถอนแล้ว**' in _SRC)
    say('㉑', '🧬 v1.1 · q194 ต้องถูกถอนออกจาก KNOWN_DUPES จริง (⛔ ไม่ใช่แค่เขียนคอมเมนต์)',
        'chap-13-calculus-q194' not in KNOWN_DUPES)

    # ── ③ 🧬 มิวแทนต์ — พิสูจน์ว่าด่านนี้รับน้ำหนักจริง ⛔ ไม่ใช่พิธีกรรม
    # 🔑 v1.3 · มิวแทนต์ทั้งกลุ่มนี้ยืนบน **ฟิกซ์เจอร์สังเคราะห์** ⛔ ไม่ใช่ KNOWN_DUPES ตัวจริง
    #    เดิม (v1.2) ยืมของจริงมา ⇒ วันที่หมุดตัวสุดท้ายถูกถอน มิวแทนต์กลายเป็น {}
    #    ⇒ เคส "ต้องแดง" กลายเป็น "⛔ ไม่มีอะไรให้แดง" = ผ่านเงียบ ๆ 4 เคสรวด (วัดจริงแล้ว)
    say('⑩', '🧬 ถอนหมุดออกทั้งหมด ⇒ ของที่ซ้ำต้องแดง (ถ้าเขียว = ด่านนี้ไม่ได้เฝ้าอะไร)',
        len(verdict(1, FIX_EXACT, {}, ids=[])[0]) >= 1
        and len(verdict(divisor, exact, {})[0]) >= len(exact))
    say('⑪', '🧬 เติมหมุดที่ไม่มีของรองรับ ⇒ ต้องแดง (รายการย่อได้อย่างเดียว)',
        any('ไม่มีของรองรับ' in m for m in
            verdict(1, FIX_EXACT,
                    dict(FIX_PINS, **{'chap-99-ghost-q01': {
                        'pair': (0, 1), 'text': 'x',
                        'why': 'ก' * (MIN_REASON + 1), 'until': 'ไม่มีวัน'}}),
                    ids=FIX_IDS + ['chap-99-ghost-q01'])[0]))
    say('⑫', '🧬 หมุดที่คู่ดัชนีไม่ตรงของจริง ⇒ ต้องแดง',
        any('ไม่ตรงกัน' in m for m in
            verdict(1, FIX_EXACT,
                    {k: dict(v, pair=(0, 2)) for k, v in FIX_PINS.items()},
                    ids=FIX_IDS)[0]))
    say('⑬', '🧬 หมุดที่ข้อความไม่ตรงของจริง ⇒ ต้องแดง (ตัวเลือกถูกแก้แต่หมุดค้าง)',
        any('ข้อความในหมุดไม่ตรง' in m for m in
            verdict(1, FIX_EXACT,
                    {k: dict(v, text='ของปลอม') for k, v in FIX_PINS.items()},
                    ids=FIX_IDS)[0]))
    say('⑭', '🧬 เหตุผลสั้นเกิน ⇒ ต้องแดง',
        any(f'สั้นกว่า {MIN_REASON}' in m for m in
            verdict(1, FIX_EXACT,
                    {k: dict(v, why='สั้น') for k, v in FIX_PINS.items()},
                    ids=FIX_IDS)[0]))
    say('⑮', '🧬 หมุดไม่มีเงื่อนไขหมดอายุ ⇒ ต้องแดง',
        any('หมดอายุ' in m for m in
            verdict(1, FIX_EXACT,
                    {k: dict(v, until='') for k, v in FIX_PINS.items()},
                    ids=FIX_IDS)[0]))
    say('㉖', '🧬 v1.3 · ฟิกซ์เจอร์ที่ไม่ถูกมิวเทต **ต้องเขียว** '
             '— ไม่งั้นเคส ⑫–⑮ อาจแดงด้วยเหตุผลอื่นแล้วอ่านว่าผ่าน',
        verdict(1, FIX_EXACT, FIX_PINS, ids=FIX_IDS)[0] == [])
    say('⑯', '🧬 ตัวหารเป็นศูนย์ ⇒ ต้องแดง แม้ ⛔ ไม่มีของซ้ำเลย',
        any('ตัวหารเป็นศูนย์' in m for m in verdict(0, {}, {})[0]))
    say('⑰', '🧬 ข้อซ้ำใหม่ที่ ⛔ ไม่ได้ปักหมุด ⇒ ต้องแดง',
        any('🆕' in m for m in
            verdict(divisor, dict(exact, **{'chap-13-calculus-q999': [(0, 2, 'ของใหม่')]}),
                    KNOWN_DUPES)[0]))

    # ── ④ รายการหมุดต้องถูกด่าน 10 เฝ้าอยู่จริง ⛔ ไม่ใช่แค่เขียนในคอมเมนต์
    #    (⑦ก ซ้อนตัวเอง — ด่าน 14 เคยพลาดข้อนี้มาแล้ว)
    say('⑱ก', 'KNOWN_DUPE_IDS ต้องลงทะเบียนใน WATCHED ของด่าน 10',
        "'scripts/check_duplicate_choices.py', 'KNOWN_DUPE_IDS'" in cg)
    # 🔑 ⑱ข คือเคสที่ v1.0 ขาดไป — v1.0 เช็กแค่ "มีชื่อใน WATCHED" ซึ่งเป็นจริงได้
    #    ทั้งที่ด่าน 10 อ่านตัวแปรนั้นไม่ออกเลย ⇒ คำอ้างที่ไม่มีด่านเฝ้า (⑦ก)
    #    ⇒ เคสนี้ **เรียกตัวอ่านของด่าน 10 มารันจริงกับไฟล์นี้**
    _st, _v = _m.parse_list(_SRC, 'KNOWN_DUPE_IDS')
    say('⑱ข', '🔑 ตัวอ่านของด่าน 10 ต้องอ่าน KNOWN_DUPE_IDS ออกจริง (สถานะ=%s)' % _st,
        _st == 'ok' and sorted(_v or []) == sorted(KNOWN_DUPE_IDS))
    say('⑱ค', '🧬 …และต้องอ่าน KNOWN_DUPES (dict ซ้อนชั้น) **ไม่ออก** — เหตุผลที่ต้องมีรายการแบน',
        _m.parse_list(_SRC, 'KNOWN_DUPES')[0] != 'ok')
    say('㉒', 'รายการแบนต้องตรงกับหมุดจริง · และ 🧬 ถ้าไม่ตรงต้องแดง',
        verdict(divisor, exact, KNOWN_DUPES)[0] == []
        and any('ไม่ตรงกับ KNOWN_DUPES' in m for m in
                verdict(divisor, exact, KNOWN_DUPES, ids=KNOWN_DUPE_IDS + ['x'])[0]))

    # ── ⑤ 🆕 v1.3 · ด่านเฝ้า "บทเรียนของ v1.3" เอง ⛔ ไม่ให้มันเป็นแค่คอมเมนต์ (⑦ก)
    # ⚠️ ㊾ · **เขตอ้างอิงของเคสนี้ = เนื้อของ selftest() เท่านั้น** ⛔ ไม่ใช่ทั้งไฟล์
    #    เพราะหัวไฟล์ **ต้อง** อ้างลวดลายต้องห้ามเพื่ออธิบายบทเรียน และกฎ "ห้ามลบบันทึกเก่า"
    #    บังคับให้บันทึกนั้นอยู่ต่อ ⇒ ด่านที่กวาดทั้งไฟล์จะจับ **บันทึกของตัวเอง** เสมอ
    #    (เคสนี้แดงจริงตอนเขียนครั้งแรก 10 ส.ค. 69 — ⛔ ไม่ใช่การคาดเดา)
    #    และ needle ต่อขึ้นตอนรัน เพื่อให้บรรทัดของเคสนี้เอง ⛔ ไม่กลายเป็นของต้องห้าม
    import inspect
    _zone = inspect.getsource(selftest)
    _needle = 'for k, v in ' + 'KNOWN_DUPES' + '.items()'
    say('㉓', '🧬 v1.3 · มิวแทนต์ห้ามยืม KNOWN_DUPES ตัวจริงมาเป็นวัตถุทดสอบอีก '
             '(ลวดลายที่ทำให้ ⑫–⑮ ตายเงียบตอนหมุดหมด) · เขตอ้างอิง = เนื้อ selftest()',
        _needle not in _zone
        # 🧬 ตัวควบคุมเชิงบวก — พิสูจน์ว่าตัวจับยังจับได้จริง ⛔ ไม่ใช่เขียวเพราะหาไม่เจอเป็นนิสัย
        and _needle in ('{k: dict(v, pair=(0, 2)) ' + _needle + '}'))
    say('㉔', 'v1.3 · หัวไฟล์ต้องบอก **ราคาจริง** ของการอ่านต้นฉบับ (ต่อหน้า) '
             'และตัวเลือกที่ 5 ของ q75 ที่อ่านออกแล้วแต่ครูสั่งให้ยังไม่คืน',
        'ต่อหน้า' in __doc__ and '1,640' in __doc__)
    say('㉕', 'v1.3 · ประวัติหมุด q135 ที่ถอนแล้วต้องยังอยู่ในไฟล์ (ห้ามลบบันทึกเก่า) '
             '· และต้อง ⛔ ไม่อยู่ในรายการหมุดจริงแล้ว',
        "ถอน 'chap-13-calculus-q135'" in _SRC
        and 'chap-13-calculus-q135' not in KNOWN_DUPES
        and 'chap-13-calculus-q135' not in KNOWN_DUPE_IDS)

    print('ผลชุดทดสอบ:',
          f'ผ่านครบ {n_case} เคส ✅' if ok else f'ล้ม ❌ (จาก {n_case} เคส)')
    return ok


def scan_stub(choice_lists):
    """ตัวช่วยของ selftest — นับตัวหารจากลิสต์ตัวเลือกดิบ ⛔ ไม่แตะดิสก์"""
    divisor = 0
    for ch in choice_lists:
        if isinstance(ch, list) and len(ch) >= 2:
            divisor += 1
    return divisor, {}, {}


def main():
    ap = argparse.ArgumentParser(
        description=f'ด่าน 15 · ตัวเลือกซ้ำ v{CHECKER_VERSION} ({CHECKER_DATE})')
    ap.add_argument('path', nargs='?', default=str(DEFAULT_SCAN))
    ap.add_argument('--selftest', action='store_true')
    ap.add_argument('--version', action='store_true')
    a = ap.parse_args()

    if a.version:
        print(CHECKER_VERSION)
        return 0
    if a.selftest:
        return 0 if selftest() else 2

    p = Path(a.path)
    files = sorted(str(x) for x in (p.glob('*.json') if p.is_dir() else [p]))
    if not files:
        print(f'  🔴 ⛔ ไม่พบไฟล์ JSON ใน {p}')
        return 2
    return 1 if report(files) else 0


if __name__ == '__main__':
    sys.exit(main())
