/.vscode
# Cowork temp files (don't commit preview/working files)
_preview/
*.PREVIEW-*.json
# PDF ต้นฉบับ + working files (ไม่ขึ้น repo)
temp-pdf/
audit/*
# ⭐ ยกเว้น: บันทึกผล blind-solve QC ต้องขึ้น repo จริง (คำขอพอร์ทัล E→MB #22 §2 ข้อ 2)
#    🔴 6 ส.ค. 69 เจอว่า "audit/" กินทั้งโฟลเดอร์ ⇒ ไฟล์ผล QC ที่เขียนลงไปจะหายเงียบ
#    ทุกคนจะเชื่อว่า "ตัวเลขขึ้น repo แล้ว" ทั้งที่ git ไม่เคยเห็นมันเลย (ทรง ⑯)
!audit/qc-*/
docs/chap-02-logic-figure-audit.md
desktop.ini
# 🔴 10 ส.ค. 69 — เกิดจริงตอนรันด่าน 15
#    selftest ⑱ข ของด่าน 15 ใช้ importlib โหลด scripts/check_config_growth.py มารันจริง
#    ⇒ Python สร้าง scripts/__pycache__/*.pyc ทิ้งไว้ ⇒ `git add -A` กวาดมันเข้าคอมมิตด้วย
#    (MB เผลอคอมมิตไปแล้ว 1 ครั้งแล้วถอนคืน — ⛔ ไม่ใช่ความเสี่ยงเชิงทฤษฎี)
#    ⇒ เคสนี้เกิดจาก **ตัวแก้ที่ทำให้ด่านซื่อสัตย์** (v1.2) ⇒ ยิ่งต้องกันไว้
__pycache__/
*.pyc

# ── 4 ก.ย. 69 · ครูเคาะ (ใบ 131-MB-E §2 · ใบ 132-E-MB) ──────────────────
#   กันด้วยเครื่อง ⛔ ไม่ใช่ด้วยความจำของคนกดคอมมิต
data/*BAK*.json
data/*pre-merge*
answer_bias.csv
report_summary.txt
report_issues.csv
_t1run/POOL-refill-*/
out/
